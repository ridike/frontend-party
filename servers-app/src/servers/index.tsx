// import React from 'react'
import { History } from 'history'

interface ListProps {
  history: History
}

export default function List(props: ListProps) {
  return (
    <div>
      Server's list
    </div>
  )
}
